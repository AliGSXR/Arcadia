{% extends 'admin/base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Ajouter un professionnel</h1>

        <div class="card">
            <div class="card-body">
                {{ include('admin/user/_form.html.twig') }}
            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('app_user_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
